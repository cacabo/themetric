<!DOCTYPE html>
<html>
  <head>
    <!-- meta tags -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="author" content="The Metric">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <% description = content_for?(:description) ? content_for(:description) : "Measuring what's happening around the world through your voice" %>
    <meta name="description" content="<%= description %>">
    <meta name="keywords" content="<%= content_for?(:keywords) ? content_for(:keywords) : 'metric,themetric,metric,progressive,student,young,diverse,youth,international,politcs,current,affairs,foreign,policy,economics,econ' %>">

    <!-- open graph -->
    <meta property="og:image" content="<%= content_for?(:image) ? content_for(:image) : 'https://i.imgur.com/AcbOq2u.png' %>">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for?(:title) ? content_for(:title) : 'The Metric' %>">
    <meta property="og:description" content="<%= description %>">
    <meta property="og:site_name" content="The Metric">

    <!-- twitter -->
    <meta name="twitter:card" content="summary_large_image"/>
    <meta property="og:url" content="themetric.org"/>
    <meta property="twitter:url" content="themetric.org"/>
    <meta property="twitter:title" content="<%= content_for?(:title) ? content_for(:title) : 'The Metric' %>"/>
    <meta property="twitter:description" content="<%= description %>"/>
    <meta property="twitter:image" content="<%= content_for?(:image) ? content_for(:image) : 'https://i.imgur.com/AcbOq2u.png' %>"/>

    <!-- title and other tags -->
    <title><%= content_for?(:title) ? content_for(:title) : "The Metric" %></title>

    <!-- built-in rails security tags -->
    <%= csrf_meta_tags %>

    <!-- icons -->
    <script src="https://use.fontawesome.com/297cffcc6b.js"></script>

    <!-- fonts -->
    <link href="https://fonts.googleapis.com/css?family=PT+Serif:400,400i,700,700i|Palanquin+Dark:400|Palanquin:400,700" rel="stylesheet">

    <!-- styles -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- favicon -->
    <%= render 'application/favicon' %>
    <%= favicon_link_tag 'favicon/favicon.ico' %>
  </head>

  <body>
    <!-- render flash notifications if there are any -->
    <% if notice or alert %>
      <div id="alert-container" class="hidden <%= notice ? 'green' : 'red' %>">
        <% if notice %>
          <div class="info" role="alert">
            <p><%=notice%></p>
          </div>
        <% end %>
        <% if alert %>
          <div class="danger" role="alert">
            <p><%=alert%></p>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- render the navbar -->
    <%= render 'shared/nav' %>

    <!-- render page content -->
    <div class="page-content">
      <%= yield %>
    </div>

    <!-- render the footer -->
    <%= render 'shared/footer' %>

    <!-- Bootstrap scripts -->
    <script src="https://code.jquery.com/jquery-3.1.1.slim.min.js" integrity="sha384-A7FZj7v+d/sdmMqp/nOQwliLvUsJfDHW+k9Omg/a/EheAdgtzNs3hpfag6Ed950n" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.4.0/js/tether.min.js" integrity="sha384-DztdAPBWPRXSA/3eYEEUWrWCy7G5KFbe8fFjk5JAIxUYHKkDx6Qin1DkWx51bBrb" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/js/bootstrap.min.js" integrity="sha384-vBWWzlZJ8ea9aCX4pEW3rVHjgjt7zpkNpZk+02D9phzyeVkE+jo0ieGizqPLForn" crossorigin="anonymous"></script>

    <!-- render flash notifications for a few seconds on page load -->
    <% if notice or alert %>
      <script>
        $(document).ready(function () {
          // Variables
          var $spacer = $('.alert-spacer');
          var $container = $('#alert-container')

          // Transitions
          $container.delay(200).slideDown(200).delay(4000).slideUp(200);
          $spacer.delay(200).slideDown(200).delay(4000).slideUp(200);
        });
      </script>
    <% end %>

    <!-- turbolinks for visualizing loading -->
    <script type="text/javascript">
      document.addEventListener('turbolinks:load', function(event) {
        if ((typeof ga) === 'function') {
          ga('set', 'location', event.data.url);
          ga('send', 'pageview');
        }
      });
    </script>

    <!-- autosize frameowkrs -->
    <script src="https://cdn.jsdelivr.net/jquery.autosize/3.0.20/autosize.min.js"></script>

    <!-- initialize autosize on all textarea components -->
    <script type="text/javascript">
      autosize($('textarea'));
    </script>

    <!-- add styling for forms with labels -->
    <%= javascript_include_tag 'special-label.js' %>

    <% if Rails.env.production? %>
      <!-- google analytics -->
      <!-- NOTE that this only runs in production so that we don't collect
           analytics while in development -->
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-105813969-1', 'auto');
        ga('send', 'pageview');
      </script>
    <% end %>
  </body>
</html>
